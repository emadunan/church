// id_search-books__heading

document.addEventListener('DOMContentLoaded', function() {
    const mainStr = document.getElementById('id_search-criteria').textContent;
    const strArr = mainStr.split(' ');
    console.log(mainStr);
});